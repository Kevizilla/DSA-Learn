def two_sum_sorted(lst, target):
    left = 0
    right = len(lst) - 1

    while left < right:

        current_sum = ...

        if current_sum == target:
            ...

        elif current_sum < target:
            ...

        else:
            ...

    return None